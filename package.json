{
  "name": "@earthling-ui/monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --cwd packages/earthling-ui build",
    "test": "bun run --cwd packages/earthling-ui build && bun test tests/build-output.test.ts tests/eject.test.ts tests/vite-consumer.test.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6"
  },
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
