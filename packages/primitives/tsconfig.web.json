{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "customConditions": ["import"],
    "composite": true
  },
  "include": ["src", "lib"],
  "exclude": ["src/**/*.native.*", "lib/**/*.native.*"]
}
