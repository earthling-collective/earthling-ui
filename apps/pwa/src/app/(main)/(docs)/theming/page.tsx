import { WorkInProgress } from "@/components/wip";

export default async function () {
  return (
    <div className="flex flex-1 flex-col items-center justify-center pb-[61px]">
      <WorkInProgress />
    </div>
  );
}
